--- 
    - name: Deploy  User 
      hosts: all
      become: true
      become_method: sudo
      become_user: root
      tasks:
# Adding arvind user 
        - name: Create User
          user:
             name: akumar
             state: present
             create_home: true
             comment: 'Admin user'
             password: "{{ 'Uniteller@123' | password_hash('sha512') }}"
             #expires: -1  # not working with me 
         
# #  copy ssh key  if  required in feature 
## Modify according to usages 
#
#         - name: Install SSH Key 
#           authorized_key: 
#                 user: ansible
#                 state: present 
#                 manage_dir: true 
#                 key: "{{lookup ('file', '/home/ansible/.sshd/id_rsa.pub') }}" 

         
# Assigning sudo access to arvind user                 
        - name: Setup arvind Sudo Access
          copy:
            dest: /etc/sudoers.d/akumar
            content: 'akumar ALL=(ALL)  ALL'
            validate: /usr/sbin/visudo -cf %s      # Verifying the sudoers file         
            
         

